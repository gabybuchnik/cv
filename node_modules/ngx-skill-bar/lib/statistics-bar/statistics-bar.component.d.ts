import { OnChanges, ChangeDetectorRef, OnInit } from "@angular/core";
export declare class StatisticsBarComponent implements OnInit, OnChanges {
    private cdr;
    width: number;
    bgColor: string;
    color: string;
    barWidth: number;
    constructor(cdr: ChangeDetectorRef);
    ngOnInit(): void;
    ngOnChanges(): void;
}
