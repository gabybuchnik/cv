import { OnInit, OnChanges } from "@angular/core";
import { Bar } from "./bar.model";
export declare class NgxSkillBarComponent implements OnInit, OnChanges {
    percentage: number;
    numberOfBars: number;
    showFigures: boolean;
    bgColor: string;
    color: string;
    barWidth: number;
    full: boolean;
    standarWidthBar: number;
    bars: Bar[];
    constructor();
    ngOnChanges(): void;
    ngOnInit(): void;
    init(): void;
    generateLine(): Bar[];
    calculateBar(indexBar: number): number;
}
