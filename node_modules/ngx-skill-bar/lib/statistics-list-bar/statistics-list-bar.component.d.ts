import { OnInit, OnChanges } from "@angular/core";
import { Bar } from "../bar.model";
export declare class StatisticsListBarComponent implements OnChanges, OnInit {
    bars: Bar[];
    showFigures: boolean;
    percentage: number;
    bgColor: string;
    color: string;
    barWidth: number;
    full: boolean;
    padding: number;
    constructor();
    ngOnInit(): void;
    ngOnChanges(): void;
}
